# Estudo de automacao em APIs com Frisby.

```ruby
Instalar o Node.Js (http://nodejs.org/);
Instalar o Jasmine (via curl ou npm install -g jasmine-node);
Instalar o Frisby (via curl ou acessar o site http://frisbyjs.com/.

Olhar a documentação: http://frisbyjs.com/docs/api/

#VaiVirarForAll
```

